server:
  port: 8080

spring:
  application:
    name: trading-backend-java
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

trading:
  python-backend:
    base-url: http://localhost:8000